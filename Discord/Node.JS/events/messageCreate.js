const client = require("../index");

client.on("messageCreate", async (message) => {
    /*if(CheckIfAllowedGuild(message.guild) && IsLogsChannel(message.channel)){
      initContent(message);
    }else{
      return
    }*/
      
});
